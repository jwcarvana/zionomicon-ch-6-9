{
  "name": "marp-mdoc-example",
  "version": "1.0.0",
  "description": "Example generating Scala Presentations",
  "main": "index.js",
  "scripts": {
    "build": "scala-cli slides.sc; marp slides-scala.md -o build/index.html; cp -r assets build/assets",
    "server": "marp --server ."
  },
  "dependencies": {
    "@marp-team/marp-cli": "^2.3.0"
  }
}
